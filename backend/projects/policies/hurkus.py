"""Hürkuş DCC extension point pending the original project rules."""


def control_hurkus_dcc(render_context):
    """Keep every captured panel unchanged until the supplied policy is available."""

    return render_context
