from django.apps import AppConfig


class MediaToolsConfig(AppConfig):
    """Configure the media conversion Django app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "media_tools"
