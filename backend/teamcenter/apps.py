from django.apps import AppConfig


class TeamcenterConfig(AppConfig):
    """Configure the Teamcenter integration application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "teamcenter"
