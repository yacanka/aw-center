import { JiraFieldInputType } from '@/models/jira'

/** Maps JIRA custom field identifiers to fixed subtask generator input types. */
export const jiraCustomFieldInputTypes: Record<string, JiraFieldInputType> = {
  // Example: customfield_12345: 'person'
}
